services:

  rust_backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rust
    ports:
      - "8080:8080"
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ./.env:/app/.env:ro
      - ./db/:/app/db/
    restart: unless-stopped
