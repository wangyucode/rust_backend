services:

  rust_backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rust
    ports:
      - "8080:8080"
    volumes:
      - ./.env:/app/.env:ro
      - ./data/:/app/data/
      - ${WWW_ROOT:-/srv/www}:/www:ro
    restart: unless-stopped
